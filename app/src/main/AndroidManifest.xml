<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- internet -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- localização GPS -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- localização GPS -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" /> <!-- camera -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- microfone -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- notificações -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trilhando"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.details.WalkDetailsActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.audio.AudioRecordActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.camera.CameraActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.walk.FinishWalkActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.walk.StartWalkActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".ui.historico.HistoryActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.trilhando.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".receiver.NotificationReceiver"
            android:enabled="true" />

    </application>

</manifest>